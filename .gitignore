node_modules
web/node_modules
*.log
.DS_Store
# Playwright CLI output (may contain credentials)
.playwright-cli/
# GitHub Pages site: build output and the precomputed demo session (bun run build:site)
site/dist/
site/.build/
